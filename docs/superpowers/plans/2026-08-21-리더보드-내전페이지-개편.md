# 리더보드/내전페이지 개편 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 리더보드를 최근 16매치 창으로 넓히고, 관리자 검색·등수 변화 화살표·team-builder 01의 종합점수 배지 4가지를 추가한다.

**Architecture:** 기존 `lib/rankingStats.ts`(순수 계산 + Supabase 조회)를 확장하고, 등수 변화 추적은 새 `lib/rankingSnapshot.ts`(순수 계산 + 조회/캡처)로 분리한다. 캡처는 03 폴링 라우트가 라운드 4개 도달을 감지했을 때 서버에서 직접 함수 호출로 트리거한다(HTTP 왕복 없음). UI는 기존 서버 컴포넌트(`app/dashboard/page.tsx`)가 데이터를 fetch해 클라이언트 컴포넌트에 prop으로 내려주는 기존 패턴을 그대로 따른다.

**Tech Stack:** Next.js 14 App Router, TypeScript, Supabase(Postgres), Vitest + Testing Library.

## Global Constraints

- 등수 변화 추적은 **종합점수(rageScore) 지표만** 대상으로 한다 — 평균등수/평균킬 탭은 화살표를 표시하지 않는다.
- 등수 스냅샷 갱신은 **03 폴링이 내전 세션(라운드 4개)의 도달을 처음 감지했을 때 자동으로** 이뤄진다 — 수동 버튼 없음.
- 검색 UI는 **관리자(`useAdmin().isAdmin === true`)에게만** 노출된다.
- 01 네임플레이트 종합점수 배지는 **관리자 전용, 최근 16매치 기준**이며 카드 왼쪽 아래 모서리에 배치한다(오른쪽 위 VipCrown·오른쪽 중간 X 삭제 버튼과 안 겹치게).
- DB 쓰기는 항상 `getSupabaseServer()`(service role)로만 한다 — anon/authenticated 키로는 `ranking_snapshots`/`scrim_rosters`에 못 쓴다(RLS에 INSERT/UPDATE 정책을 안 만듦, 0016 패턴과 동일).
- 마이그레이션은 `node scripts/apply-migration.mjs <파일>`로 적용하고 `node scripts/verify-schema.mjs`로 검증한다(이 저장소의 기존 운영 방식).
- 스펙 문서: `docs/superpowers/specs/2026-08-21-리더보드-내전페이지-개편-design.md`

---

## Task 1: 최근 12매치 → 16매치 리네이밍 + 공용 티어 밴드 상수

**Files:**
- Modify: `lib/rankingStats.ts`
- Modify: `lib/rankingStats.test.ts`
- Modify: `components/dashboard/TierRankingPodium.tsx`
- Modify: `components/dashboard/TierRankingPodium.test.tsx`
- Modify: `app/dashboard/page.tsx`
- Create: `supabase/migrations/0021_recent_ranking_window_16.sql`
- Modify: `scripts/verify-schema.mjs`

**Interfaces:**
- Produces: `export const MIN_GAMES_FOR_RANKING = 16` (`lib/rankingStats.ts`), `export type RankingWindow = 'recent16' | 'alltime'`, `export const TIER_SCORE_BANDS: number[][]` (`lib/rankingStats.ts`) — 이후 태스크가 그룹별 순위·01 점수 계산에 재사용한다.

- [ ] **Step 1: 실패하는 테스트부터 고친다 — `lib/rankingStats.test.ts`**

`MIN_GAMES_FOR_RANKING` 테스트를 16으로 바꾼다:

```ts
describe('MIN_GAMES_FOR_RANKING', () => {
  it('16경기(내전 4회)다', () => {
    expect(MIN_GAMES_FOR_RANKING).toBe(16);
  });
});
```

같은 파일 맨 아래에 `TIER_SCORE_BANDS` import를 추가하고 테스트를 하나 더 붙인다:

```ts
import {
  ACTIVE_WITHIN_MONTHS,
  MIN_GAMES_FOR_RANKING,
  RAGE_SCORE_STEEPNESS,
  TIER_SCORE_BANDS,
  eligibleForRanking,
  rageScores,
  topByAvgKills,
  topByAvgRank,
  topByRageScore,
  type RankingStatsRow,
} from './rankingStats';
```

```ts
describe('TIER_SCORE_BANDS', () => {
  it('전체 탭을 뺀 티어 그룹 5개와 같은 개수다', () => {
    expect(TIER_SCORE_BANDS.length).toBe(5);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npx vitest run lib/rankingStats.test.ts`
Expected: FAIL — `MIN_GAMES_FOR_RANKING`가 아직 12이고 `TIER_SCORE_BANDS`가 없어서 컴파일 에러.

- [ ] **Step 3: `lib/rankingStats.ts` 수정**

파일 상단 import에 `TIER_GROUPS` 추가:

```ts
import { getSupabase } from './supabaseBrowser';
import { cleanDisplayName, fetchAllMembers } from './memberStats';
import { TIER_GROUPS } from './dashboardData';
```

`MIN_GAMES_FOR_RANKING`을 16으로:

```ts
// 내전 4회(하루 4경기 기준 16경기) 미만이면 랭킹에서 뺀다.
// 1~2경기짜리 우연을 실력처럼 보여주는 걸 막는다.
export const MIN_GAMES_FOR_RANKING = 16;
```

`totalScore`/`tierBandIndexOf` 함수 사이 아무 곳(예: `rageScores` 함수 바로 위)에 공용 밴드 상수를 추가한다 — `TierRankingPodium.tsx`의 기존 `TIER_BANDS` 계산을 그대로 옮긴 것이다:

```ts
// 점수는 "자기 고정 티어 밴드" 안에서 계산한다 — 대시보드 티어 탭과 같은 경계다.
// TierRankingPodium·01 네임플레이트 배지·등수 스냅샷 캡처가 전부 이 상수를 공유한다.
export const TIER_SCORE_BANDS: number[][] = TIER_GROUPS.filter((group) => group.tiers !== null).map(
  (group) => group.tiers!,
);
```

`RankingWindow` 타입과 `fetchRankingStats` 안의 분기를 리네이밍:

```ts
export type RankingWindow = 'recent16' | 'alltime';
```

```ts
  let windowData: RankingViewRow[] = alltimeData;
  if (window === 'recent16') {
    // 6각형이 쓰는 member_recent_stats 가 아니라 랭킹 전용 뷰다 —
    // 이쪽만 스크린샷 백필까지 합쳐서 최근 16경기(내전 4회)를 센다 (0013/0021).
    const { data, error } = await getSupabase()
      .from('member_recent_ranking_stats')
      .select('member_id, tier, game_count, avg_kills, avg_placement_points, avg_rank');
    if (error) throw new Error(`최근 전적을 불러오지 못했습니다: ${error.message}`);
    windowData = data ?? [];
  }
```

- [ ] **Step 4: `components/dashboard/TierRankingPodium.tsx` 수정**

`TIER_BANDS` 로컬 계산을 지우고 공용 상수를 쓴다. 상단 import 블록:

```ts
import {
  RAGE_SCORE_STEEPNESS,
  TIER_SCORE_BANDS,
  eligibleForRanking,
  topByAvgKills,
  topByAvgRank,
  topByRageScore,
  type RankingStatsRow,
} from '@/lib/rankingStats';
```

`const TIER_BANDS = TIER_GROUPS.filter(...)` 줄을 삭제하고, 아래에서 `TIER_BANDS`를 쓰던 자리(`topByRageScore(groupRows, TIER_BANDS, RAGE_SCORE_STEEPNESS, RANKING_SIZE)`)를 `TIER_SCORE_BANDS`로 바꾼다.

`Window`/`WINDOW_OPTIONS`/`WINDOW_HELP`/`AGGREGATION_RULES`를 16매치 기준으로:

```ts
type Window = 'recent16' | 'alltime';

const WINDOW_OPTIONS: Array<{ id: Window; label: string }> = [
  { id: 'alltime', label: '역대 전체' },
  { id: 'recent16', label: '최근 16매치' },
];

const WINDOW_HELP: Array<{ term: string; desc: string }> = [
  { term: '역대 전체', desc: '40시즌부터 현재까지' },
  { term: '최근 16매치', desc: '최근 내전 4회 (본인이 참여한)' },
];
```

```ts
const AGGREGATION_RULES = [
  '통산 16경기(내전 4회) 이상 참가한 클랜원만 집계',
  '최근 3개월 이내 내전 참가 기록이 없으면 제외',
  '종합점수: 매치당 등수점수+킬 합산 성적을 같은 티어 그룹 안에서 상대평가 (그룹 평균 = 50점)',
  '평균킬·평균등수: 매치당 평균 (부계정 포함)',
  '최근 16매치 = 본인이 참여한 가장 최근 내전 4회, 역대 전체 = 40시즌부터 통산 전체 경기',
];
```

`TierRankingPodiumProps`와 컴포넌트 시그니처, 내부 참조를 리네이밍:

```ts
export interface TierRankingPodiumProps {
  recent16: RankingStatsRow[];
  alltime: RankingStatsRow[];
}
```

```ts
export function TierRankingPodium({ recent16, alltime }: TierRankingPodiumProps) {
  ...
  const [activeWindow, setActiveWindow] = useState<Window>('recent16');
  ...
  const rows = activeWindow === 'recent16' ? recent16 : alltime;
```

- [ ] **Step 5: `app/dashboard/page.tsx` 수정**

```tsx
export default async function DashboardPage() {
  const [recent16, alltime] = await Promise.all([
    fetchRankingStats('recent16'),
    fetchRankingStats('alltime'),
  ]);

  return (
    <main className="min-h-screen">
      <Nav />
      <h1 className="sr-only">{siteConfig.dashboard.pageHeading}</h1>
      <TierRankingPodium recent16={recent16} alltime={alltime} />
      <Footer />
    </main>
  );
}
```

- [ ] **Step 6: `components/dashboard/TierRankingPodium.test.tsx` 수정**

`RECENT10` → `RECENT16`으로 이름 바꾸고(내용은 그대로), 모든 `<TierRankingPodium recent12={...} .../>` 호출을 `recent16={...}`로, 테스트 설명 문구의 "최근12매치"를 "최근16매치"로, "통산 12경기 미만"을 "통산 16경기 미만"으로 바꾼다. 예:

```tsx
const RECENT16: RankingStatsRow[] = [
  row({ memberId: 'a', discordNickname: 'A', tier: 0, totalGameCount: 20, avgKills: 3, avgPlacementPoints: 8 }),
  // ... 기존 항목 그대로
];
```

```tsx
it('기본값(전체/종합점수/최근16매치)으로 점수 내림차순 top3를 채우고 티어 배지를 보여준다', () => {
  render(<TierRankingPodium recent16={RECENT16} alltime={ALLTIME} />);
  ...
```

파일 안의 모든 `recent12={RECENT10}` → `recent16={RECENT16}`로 일괄 치환한다(총 4곳: 기본 렌더 테스트, 자격미달 테스트, 평균킬 탭 테스트, 관리자 제한 테스트).

- [ ] **Step 7: 마이그레이션 작성 — `supabase/migrations/0021_recent_ranking_window_16.sql`**

```sql
-- 내전이 주 2회로 늘어나면서 "최근 12매치(내전 3회)" 창이 실제 운영 빈도보다
-- 좁아졌다. 16매치(내전 4회)로 넓힌다. 0013과 같은 구조, rn 상한만 바꾼다.

create or replace view member_recent_ranking_stats as
with ranked as (
  select
    member_id,
    kills,
    team_rank,
    row_number() over (partition by member_id order by played_at desc) as rn
  from member_ranking_games
)
select
  r.member_id,
  mem.tier,
  count(*)::integer as game_count,
  avg(r.kills) as avg_kills,
  avg(placement_points(r.team_rank)) as avg_placement_points
from ranked r
join members mem on mem.id = r.member_id
where r.rn <= 16
group by r.member_id, mem.tier;
```

- [ ] **Step 8: 마이그레이션 적용 + 검증 블록 추가**

Run: `node scripts/apply-migration.mjs supabase/migrations/0021_recent_ranking_window_16.sql`
Expected: `적용 완료: supabase/migrations/0021_recent_ranking_window_16.sql`

`scripts/verify-schema.mjs` 맨 끝(`await client.end();` 바로 위)에 추가:

```js
console.log('\n0021 — 최근 랭킹 창 12매치 → 16매치');

check(
  (await one(`select count(*) from information_schema.view_column_usage
    where view_name = 'member_recent_ranking_stats'`)) > 0,
  'member_recent_ranking_stats 뷰가 존재한다',
);
```

Run: `node scripts/verify-schema.mjs`
Expected: `0021 — 최근 랭킹 창 12매치 → 16매치` 아래 `OK` 두 줄, 마지막에 `스키마 검증 통과`

- [ ] **Step 9: 타입체크 + 테스트 실행**

Run: `npx tsc --noEmit && npx vitest run lib/rankingStats.test.ts components/dashboard/TierRankingPodium.test.tsx`
Expected: 타입 에러 없음, 모든 테스트 PASS

- [ ] **Step 10: 커밋**

```bash
git add lib/rankingStats.ts lib/rankingStats.test.ts components/dashboard/TierRankingPodium.tsx components/dashboard/TierRankingPodium.test.tsx app/dashboard/page.tsx supabase/migrations/0021_recent_ranking_window_16.sql scripts/verify-schema.mjs
git commit -m "feat: widen ranking window from 12 to 16 matches, extract shared tier score bands"
```

---

## Task 2: 관리자 전용 리더보드 검색

**Files:**
- Modify: `components/dashboard/TierRankingPodium.tsx`
- Modify: `components/dashboard/TierRankingPodium.test.tsx`

**Interfaces:**
- Consumes: Task 1의 `useAdmin()`(이미 컴포넌트에 있음), `restRanked`(기존 지역 변수).
- Produces: 없음(순수 UI, 다른 태스크가 의존하지 않음).

- [ ] **Step 1: 실패하는 테스트 작성 — `TierRankingPodium.test.tsx`**

파일 끝(마지막 `});` 앞, 관리자 제한 테스트 다음)에 추가:

```tsx
describe('TierRankingPodium — 관리자 검색', () => {
  it('일반 사용자에게는 검색창이 안 보인다', () => {
    render(<TierRankingPodium recent16={RECENT16} alltime={ALLTIME} />);
    expect(screen.queryByPlaceholderText('닉네임 검색')).not.toBeInTheDocument();
  });

  it('관리자는 검색창으로 4위 이하 표를 닉네임 부분일치로 필터링할 수 있다', async () => {
    vi.mocked(useAdmin).mockReturnValue({ isAdmin: true, login: vi.fn(), logout: vi.fn() });
    render(<TierRankingPodium recent16={RECENT16} alltime={ALLTIME} />);

    const input = screen.getByPlaceholderText('닉네임 검색');
    fireEvent.change(input, { target: { value: 'f' } });

    expect(screen.getByText('F')).toBeInTheDocument();
    expect(screen.queryByText('E')).not.toBeInTheDocument();
  });

  it('검색 결과가 없으면 안내 문구를 보여준다', () => {
    vi.mocked(useAdmin).mockReturnValue({ isAdmin: true, login: vi.fn(), logout: vi.fn() });
    render(<TierRankingPodium recent16={RECENT16} alltime={ALLTIME} />);

    fireEvent.change(screen.getByPlaceholderText('닉네임 검색'), { target: { value: '없는이름' } });
    expect(screen.getByText('검색 결과가 없습니다')).toBeInTheDocument();
  });

  it('검색 중에도 시상대(top3)는 그대로 남는다', () => {
    vi.mocked(useAdmin).mockReturnValue({ isAdmin: true, login: vi.fn(), logout: vi.fn() });
    render(<TierRankingPodium recent16={RECENT16} alltime={ALLTIME} />);

    fireEvent.change(screen.getByPlaceholderText('닉네임 검색'), { target: { value: '없는이름' } });
    expect(within(screen.getByTestId('podium-slot-1')).getByText('D')).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run components/dashboard/TierRankingPodium.test.tsx -t "관리자 검색"`
Expected: FAIL — 검색창(`placeholder="닉네임 검색"`)이 아직 없음.

- [ ] **Step 3: 구현 — `TierRankingPodium.tsx`**

`useState` import는 이미 있다. 컴포넌트 안, 다른 `useState` 선언들 옆에 추가:

```ts
const [searchQuery, setSearchQuery] = useState('');
```

`{restRanked.length > 0 && (...)}` 블록 전체를 아래로 교체한다(기존 헤더 grid·행 렌더링·총원 문구는 그대로 옮기고, 검색창과 필터링 로직만 새로 끼워 넣는다):

```tsx
{restRanked.length > 0 && (
  <div className="mx-auto mt-10 max-w-4xl">
    {isAdmin && (
      <div className="mb-2 flex justify-end px-4">
        <input
          type="text"
          value={searchQuery}
          onChange={(event) => setSearchQuery(event.target.value)}
          placeholder="닉네임 검색"
          className="rounded-md border border-white/15 bg-white/[0.03] px-3 py-1.5 text-sm text-foreground placeholder:text-menu focus:border-accent focus:outline-none"
        />
      </div>
    )}

    <div className={`${RANKING_GRID} px-4 pb-2 text-xs text-white/60`}>
      <span>등수</span>
      <span>닉네임</span>
      <span>티어</span>
      <span className="text-center">뱃지</span>
      <span className="text-right">{METRIC_OPTIONS.find((o) => o.id === activeMetric)?.label}</span>
    </div>

    {(() => {
      const query = searchQuery.trim().toLowerCase();
      const displayedRanked =
        isAdmin && query
          ? restRanked.filter((member) => member.discordNickname.toLowerCase().includes(query))
          : restRanked;

      if (displayedRanked.length === 0) {
        return <p className="mt-4 text-center text-sm text-menu">검색 결과가 없습니다</p>;
      }

      return (
        <div className="space-y-1">
          {displayedRanked.map((member) => {
            const originalRank = restRanked.indexOf(member) + 4;
            return (
              <div
                key={member.memberId}
                data-testid={`ranking-row-${originalRank}`}
                className={`${RANKING_GRID} rounded-xl px-4 py-2.5`}
                style={{ background: RANKING_ROW_BG }}
              >
                <span className="text-sm font-bold text-menu">{originalRank}</span>
                <span className="min-w-0 truncate text-sm font-bold text-foreground">
                  {member.discordNickname}
                </span>
                <span className="text-sm text-menu">{member.tier}티어</span>
                <span className="text-center text-sm text-menu">{badgeLabel(member)}</span>
                <span className="text-right tabular-nums">
                  <MetricValue
                    metric={activeMetric}
                    row={member}
                    className="text-sm font-bold text-foreground"
                    detailClassName="text-xs font-normal text-menu"
                  />
                </span>
              </div>
            );
          })}
        </div>
      );
    })()}

    <p className="mt-3 text-right text-xs text-menu">
      총 {groupRows.length}명 중 {topRanked.length}명
    </p>
  </div>
)}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run components/dashboard/TierRankingPodium.test.tsx`
Expected: 전체 PASS

- [ ] **Step 5: 커밋**

```bash
git add components/dashboard/TierRankingPodium.tsx components/dashboard/TierRankingPodium.test.tsx
git commit -m "feat: add admin-only nickname search to leaderboard table"
```

---

## Task 3: 등수 스냅샷 스키마

**Files:**
- Create: `supabase/migrations/0022_ranking_snapshots.sql`
- Modify: `scripts/verify-schema.mjs`

**Interfaces:**
- Produces: 테이블 `ranking_snapshots(window, group_id, member_id, rank_position, captured_at)`, 컬럼 `scrim_rosters.ranking_snapshot_captured_at` — Task 4/5가 이 스키마에 읽고 쓴다.

- [ ] **Step 1: 마이그레이션 작성**

```sql
-- 내전(4매치) 세션이 끝날 때마다 종합점수 등수 스냅샷을 남겨, 리더보드에서
-- 직전 세션 대비 등수 변화(상승/하락/신규)를 보여준다.
--
-- group_id 는 lib/dashboardData.ts 의 TIER_GROUPS.id 를 그대로 쓴다('all' +
-- 티어 그룹 5개) — 리더보드 탭 구조와 정확히 대응시켜 어느 탭에서 보든 그
-- 탭 기준 등수 변화가 나오게 한다. unique 제약이라 캡처마다 upsert로 이전
-- 값을 덮어써서 항상 "가장 최근 세션 이전" 스냅샷 하나만 남는다.

create table if not exists ranking_snapshots (
  id uuid primary key default gen_random_uuid(),
  window text not null check (window in ('recent16', 'alltime')),
  group_id text not null,
  member_id uuid not null references members(id) on delete cascade,
  rank_position integer not null,
  captured_at timestamptz not null default now(),
  unique (window, group_id, member_id)
);

alter table ranking_snapshots enable row level security;
create policy "ranking_snapshots_select_public" on ranking_snapshots for select using (true);

-- 쓰기(INSERT/UPDATE)는 0016 패턴과 같은 방식으로 service role 로만 한다 —
-- 이 정책들과 무관하게, INSERT/UPDATE 정책을 안 만들었으므로 anon/authenticated
-- 키로는 애초에 쓸 수 없다.
grant select on ranking_snapshots to anon;
grant select on ranking_snapshots to authenticated;

-- 이 로스터(내전 세션)에서 스냅샷을 이미 캡처했는지 표시하는 1회성 플래그.
-- 서버(service role)만 읽고 쓰므로 anon/authenticated 에는 따로 안 가린다
-- (0004 의 컬럼 단위 grant 는 클라이언트가 읽어야 하는 컬럼에만 필요하다).
alter table scrim_rosters add column if not exists ranking_snapshot_captured_at timestamptz;
```

- [ ] **Step 2: 마이그레이션 적용**

Run: `node scripts/apply-migration.mjs supabase/migrations/0022_ranking_snapshots.sql`
Expected: `적용 완료: supabase/migrations/0022_ranking_snapshots.sql`

- [ ] **Step 3: 검증 블록 추가 — `scripts/verify-schema.mjs`**

파일 끝(`await client.end();` 바로 위)에 추가:

```js
console.log('\n0022 — 등수 스냅샷');

check(
  (await one(`select count(*) from information_schema.tables
    where table_name = 'ranking_snapshots'`)) === 1,
  'ranking_snapshots 테이블이 있다',
);

check(
  (await one(`select count(*) from pg_constraint
    where conrelid = 'ranking_snapshots'::regclass and contype = 'u'
      and pg_get_constraintdef(oid) like '%window%group_id%member_id%'`)) === 1,
  '(window, group_id, member_id) 유일 제약이 있다',
);

const snapshotGrants = await client.query(`
  select grantee from information_schema.role_table_grants
  where table_name = 'ranking_snapshots' and privilege_type = 'SELECT'
    and grantee in ('anon', 'authenticated')
`);
for (const role of ['anon', 'authenticated']) {
  check(
    snapshotGrants.rows.some((r) => r.grantee === role),
    `${role} 은 ranking_snapshots 를 읽을 수 있다`,
  );
}

check(
  (await one(`select count(*) from information_schema.columns
    where table_name = 'scrim_rosters' and column_name = 'ranking_snapshot_captured_at'`)) === 1,
  'scrim_rosters.ranking_snapshot_captured_at 컬럼이 있다',
);
```

- [ ] **Step 4: 검증 실행**

Run: `node scripts/verify-schema.mjs`
Expected: `0022 — 등수 스냅샷` 아래 전부 `OK`, 마지막 `스키마 검증 통과`

- [ ] **Step 5: 커밋**

```bash
git add supabase/migrations/0022_ranking_snapshots.sql scripts/verify-schema.mjs
git commit -m "feat: add ranking_snapshots table and scrim_rosters capture flag"
```

---

## Task 4: 등수 스냅샷 순수 계산 함수

**Files:**
- Create: `lib/rankingSnapshot.ts`
- Create: `lib/rankingSnapshot.test.ts`

**Interfaces:**
- Consumes: `RankingStatsRow`, `RankingWindow`, `TIER_SCORE_BANDS`, `RAGE_SCORE_STEEPNESS`, `eligibleForRanking`, `topByRageScore`(전부 `lib/rankingStats.ts`), `TIER_GROUPS`(`lib/dashboardData.ts`).
- Produces: `export interface RankingSnapshotRow { window: RankingWindow; groupId: string; memberId: string; rankPosition: number }`, `export function computeSnapshotRows(recent16Rows: RankingStatsRow[], alltimeRows: RankingStatsRow[]): RankingSnapshotRow[]`, `export type RankChange = { type: 'new' } | { type: 'up'; delta: number } | { type: 'down'; delta: number }`, `export function computeRankChange(currentRank: number, previousRank: number | undefined): RankChange | null` — Task 5(캡처)와 Task 6(화면 표시)이 그대로 가져다 쓴다.

- [ ] **Step 1: 실패하는 테스트 작성 — `lib/rankingSnapshot.test.ts`**

```ts
import { describe, expect, it } from 'vitest';
import { computeRankChange, computeSnapshotRows } from './rankingSnapshot';
import type { RankingStatsRow } from './rankingStats';

function row(overrides: Partial<RankingStatsRow> = {}): RankingStatsRow {
  return {
    memberId: 'm-1',
    discordNickname: 'Member1',
    tier: 0,
    totalGameCount: 20,
    windowGameCount: 20,
    avgKills: 2,
    avgPlacementPoints: 4,
    avgRank: 5,
    lastPlayedAt: new Date().toISOString(),
    ...overrides,
  };
}

describe('computeSnapshotRows', () => {
  it('전체(all) 그룹과 그 사람의 티어 그룹 양쪽에 순위를 낸다', () => {
    const rows = [
      row({ memberId: 'a', tier: 0, avgPlacementPoints: 8 }),
      row({ memberId: 'b', tier: 0, avgPlacementPoints: 4 }),
      row({ memberId: 'c', tier: 2, avgPlacementPoints: 100 }),
    ];
    const result = computeSnapshotRows(rows, rows);

    const recentAll = result.filter((r) => r.window === 'recent16' && r.groupId === 'all');
    expect(recentAll.find((r) => r.memberId === 'a')?.rankPosition).toBe(1);

    const recentTier0 = result.filter((r) => r.window === 'recent16' && r.groupId === '0-1.5');
    expect(recentTier0.map((r) => r.memberId).sort()).toEqual(['a', 'b']);

    const recentTier2 = result.filter((r) => r.window === 'recent16' && r.groupId === '2-2.5');
    expect(recentTier2.map((r) => r.memberId)).toEqual(['c']);
  });

  it('자격 미달(경기수 부족)인 사람은 어느 그룹에도 안 낀다', () => {
    const rows = [row({ memberId: 'a', totalGameCount: 3 })];
    expect(computeSnapshotRows(rows, rows)).toEqual([]);
  });

  it('recent16과 alltime을 따로 계산한다', () => {
    const recentRows = [row({ memberId: 'a', avgPlacementPoints: 10 })];
    const alltimeRows = [row({ memberId: 'a', avgPlacementPoints: 2 })];
    const result = computeSnapshotRows(recentRows, alltimeRows);
    expect(result.filter((r) => r.window === 'alltime').map((r) => r.memberId)).toContain('a');
    expect(result.filter((r) => r.window === 'recent16').map((r) => r.memberId)).toContain('a');
  });
});

describe('computeRankChange', () => {
  it('이전 스냅샷에 없으면 신규다', () => {
    expect(computeRankChange(3, undefined)).toEqual({ type: 'new' });
  });

  it('순위 숫자가 작아지면(상승) up과 상승폭을 낸다', () => {
    expect(computeRankChange(2, 5)).toEqual({ type: 'up', delta: 3 });
  });

  it('순위 숫자가 커지면(하락) down과 하락폭을 낸다', () => {
    expect(computeRankChange(7, 5)).toEqual({ type: 'down', delta: 2 });
  });

  it('순위가 같으면 아무것도 안 낸다', () => {
    expect(computeRankChange(4, 4)).toBeNull();
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run lib/rankingSnapshot.test.ts`
Expected: FAIL — `lib/rankingSnapshot.ts` 모듈이 아직 없음.

- [ ] **Step 3: 구현 — `lib/rankingSnapshot.ts`**

```ts
// 등수 스냅샷 — 내전 세션이 끝날 때마다 종합점수 순위를 저장해뒀다가, 다음에
// 볼 때 상승/하락/신규를 비교하는 데 쓴다. 순수 계산은 이 파일에서(Supabase
// 없이 테스트), 실제 조회/캡처는 파일 뒷부분(export async 함수)에만 있다.

import type { SupabaseClient } from '@supabase/supabase-js';
import { getSupabase } from './supabaseBrowser';
import { TIER_GROUPS } from './dashboardData';
import {
  RAGE_SCORE_STEEPNESS,
  TIER_SCORE_BANDS,
  eligibleForRanking,
  fetchRankingStats,
  topByRageScore,
  type RankingStatsRow,
  type RankingWindow,
} from './rankingStats';

export interface RankingSnapshotRow {
  window: RankingWindow;
  groupId: string;
  memberId: string;
  rankPosition: number;
}

// group_id 는 TIER_GROUPS.id 그대로 쓴다('all' + 티어 그룹 5개) — 리더보드
// 탭과 정확히 대응시켜, 어느 탭에서 보든 그 탭 기준 등수 변화가 나오게 한다.
export function computeSnapshotRows(
  recent16Rows: RankingStatsRow[],
  alltimeRows: RankingStatsRow[],
): RankingSnapshotRow[] {
  const byWindow: Array<[RankingWindow, RankingStatsRow[]]> = [
    ['recent16', recent16Rows],
    ['alltime', alltimeRows],
  ];
  const result: RankingSnapshotRow[] = [];

  for (const [window, rows] of byWindow) {
    const eligible = eligibleForRanking(rows);
    for (const group of TIER_GROUPS) {
      const groupRows =
        group.tiers === null ? eligible : eligible.filter((row) => group.tiers!.includes(row.tier));
      const ranked = topByRageScore(groupRows, TIER_SCORE_BANDS, RAGE_SCORE_STEEPNESS, groupRows.length);
      ranked.forEach((row, index) => {
        result.push({ window, groupId: group.id, memberId: row.memberId, rankPosition: index + 1 });
      });
    }
  }
  return result;
}

export type RankChange = { type: 'new' } | { type: 'up'; delta: number } | { type: 'down'; delta: number };

export function computeRankChange(currentRank: number, previousRank: number | undefined): RankChange | null {
  if (previousRank === undefined) return { type: 'new' };
  if (currentRank < previousRank) return { type: 'up', delta: previousRank - currentRank };
  if (currentRank > previousRank) return { type: 'down', delta: currentRank - previousRank };
  return null;
}

// 화면(TierRankingPodium)이 읽는 쪽 — anon 키로 읽기만 한다.
export async function fetchRankingSnapshots(): Promise<RankingSnapshotRow[]> {
  const { data, error } = await getSupabase()
    .from('ranking_snapshots')
    .select('window, group_id, member_id, rank_position');
  if (error) throw new Error(`등수 스냅샷을 불러오지 못했습니다: ${error.message}`);
  return (data ?? []).map((row) => ({
    window: row.window as RankingWindow,
    groupId: row.group_id as string,
    memberId: row.member_id as string,
    rankPosition: row.rank_position as number,
  }));
}

// 03 폴링이 라운드 4개 도달을 감지했을 때 호출한다(캡처 API 라우트도 이
// 함수를 그대로 가져다 쓴다). 이미 캡처된 로스터면 아무것도 안 하고
// { captured: false } 를 낸다 — 여러 번 호출돼도 안전하다.
export async function captureRankingSnapshotForRoster(
  supabase: SupabaseClient,
  rosterId: string,
): Promise<{ captured: boolean }> {
  const { data: rosterRow, error: rosterError } = await supabase
    .from('scrim_rosters')
    .select('ranking_snapshot_captured_at')
    .eq('id', rosterId)
    .maybeSingle();
  if (rosterError) throw new Error(`로스터를 조회하지 못했습니다: ${rosterError.message}`);
  if (!rosterRow || rosterRow.ranking_snapshot_captured_at) {
    return { captured: false };
  }

  const [recent16Rows, alltimeRows] = await Promise.all([
    fetchRankingStats('recent16'),
    fetchRankingStats('alltime'),
  ]);
  const rows = computeSnapshotRows(recent16Rows, alltimeRows);

  if (rows.length > 0) {
    const { error: upsertError } = await supabase.from('ranking_snapshots').upsert(
      rows.map((row) => ({
        window: row.window,
        group_id: row.groupId,
        member_id: row.memberId,
        rank_position: row.rankPosition,
        captured_at: new Date().toISOString(),
      })),
      { onConflict: 'window,group_id,member_id' },
    );
    if (upsertError) throw new Error(`등수 스냅샷 저장에 실패했습니다: ${upsertError.message}`);
  }

  const { error: updateError } = await supabase
    .from('scrim_rosters')
    .update({ ranking_snapshot_captured_at: new Date().toISOString() })
    .eq('id', rosterId);
  if (updateError) throw new Error(`캡처 시각 저장에 실패했습니다: ${updateError.message}`);

  return { captured: true };
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run lib/rankingSnapshot.test.ts`
Expected: 전체 PASS

- [ ] **Step 5: 타입체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 6: 커밋**

```bash
git add lib/rankingSnapshot.ts lib/rankingSnapshot.test.ts
git commit -m "feat: add pure ranking snapshot/rank-change computation"
```

---

## Task 5: 캡처 API 라우트 + 03 폴링 연동

**Files:**
- Create: `app/api/ranking-snapshots/capture/route.ts`
- Create: `app/api/ranking-snapshots/capture/route.test.ts`
- Modify: `app/api/scrim-roster/round-sheet/poll/route.ts`

**Interfaces:**
- Consumes: `captureRankingSnapshotForRoster`(Task 4, `lib/rankingSnapshot.ts`), `getSupabaseServer`(`lib/supabaseServer.ts`), 기존 `runPolling`(`supabase/functions/_shared/polling.mjs`).
- Produces: `POST /api/ranking-snapshots/capture` — `{ rosterId }` → `{ captured: boolean }` | `{ error: string }`.

- [ ] **Step 1: 캡처 라우트 구현 — `app/api/ranking-snapshots/capture/route.ts`**

```ts
import { NextResponse } from 'next/server';
import { getSupabaseServer } from '@/lib/supabaseServer';
import { captureRankingSnapshotForRoster } from '@/lib/rankingSnapshot';

// 03 폴링이 라운드 4개 도달을 감지했을 때 서버 내부에서 직접 호출하는
// 함수(captureRankingSnapshotForRoster)를 그대로 감싼 HTTP 진입점 — 수동
// 재실행·테스트용으로 별도 라우트도 열어둔다.
export async function POST(request: Request) {
  const body = await request.json().catch(() => ({}));
  const rosterId = typeof body.rosterId === 'string' ? body.rosterId : null;
  if (!rosterId) {
    return NextResponse.json({ error: 'rosterId 가 필요합니다.' }, { status: 400 });
  }

  try {
    const result = await captureRankingSnapshotForRoster(getSupabaseServer(), rosterId);
    return NextResponse.json(result);
  } catch (error) {
    const message = error instanceof Error ? error.message : '등수 스냅샷 캡처에 실패했습니다.';
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
```

- [ ] **Step 2: 라우트 테스트 작성 — `app/api/ranking-snapshots/capture/route.test.ts`**

```ts
import { describe, expect, it, vi } from 'vitest';
import { POST } from './route';

vi.mock('@/lib/supabaseServer', () => ({ getSupabaseServer: () => ({}) }));
vi.mock('@/lib/rankingSnapshot', () => ({
  captureRankingSnapshotForRoster: vi.fn().mockResolvedValue({ captured: true }),
}));

describe('POST /api/ranking-snapshots/capture', () => {
  it('rosterId가 없으면 400을 낸다', async () => {
    const response = await POST(new Request('http://localhost', { method: 'POST', body: '{}' }));
    expect(response.status).toBe(400);
  });

  it('rosterId가 있으면 캡처 함수를 호출하고 결과를 그대로 낸다', async () => {
    const response = await POST(
      new Request('http://localhost', { method: 'POST', body: JSON.stringify({ rosterId: 'r1' }) }),
    );
    const body = await response.json();
    expect(body).toEqual({ captured: true });
  });
});
```

- [ ] **Step 3: 테스트 실행**

Run: `npx vitest run app/api/ranking-snapshots/capture/route.test.ts`
Expected: 전체 PASS

- [ ] **Step 4: 03 폴링 라우트 연동 — `app/api/scrim-roster/round-sheet/poll/route.ts`**

파일 상단 import에 추가:

```ts
import type { SupabaseClient } from '@supabase/supabase-js';
import { captureRankingSnapshotForRoster } from '@/lib/rankingSnapshot';
```

`export async function POST` 위에 헬퍼 두 개를 추가한다(`app/api/scrim-roster/round-sheet/route.ts`의 `toKstDate`·세션/매치 조회와 같은 로직을 이 라우트에서도 짧게 다시 쓴다 — 그 파일의 주석대로 "Deno/Node 공용 모듈이 아니라 짧으니 그냥 옮겨 쓰는" 이 프로젝트의 기존 패턴이다):

```ts
const KST_OFFSET_MS = 9 * 3600 * 1000;

function toKstDate(isoTimestamp: string): string {
  const kst = new Date(new Date(isoTimestamp).getTime() + KST_OFFSET_MS);
  const pad = (n: number) => String(n).padStart(2, '0');
  return `${kst.getUTCFullYear()}-${pad(kst.getUTCMonth() + 1)}-${pad(kst.getUTCDate())}`;
}

async function countRoundsForRoster(supabase: SupabaseClient, rosterId: string): Promise<number> {
  const { data: rosterRow } = await supabase
    .from('scrim_rosters')
    .select('fetched_at')
    .eq('id', rosterId)
    .maybeSingle();
  if (!rosterRow) return 0;

  const { data: session } = await supabase
    .from('scrim_sessions')
    .select('id')
    .eq('scrim_date', toKstDate(rosterRow.fetched_at as string))
    .maybeSingle();
  if (!session) return 0;

  const { count } = await supabase
    .from('matches')
    .select('id', { count: 'exact', head: true })
    .eq('scrim_session_id', session.id);
  return count ?? 0;
}
```

`runPolling` 호출 뒤, `return NextResponse.json({ found: ... })` 앞에 삽입:

```ts
    const result = await runPolling({
      supabase,
      apiKey,
      sinceHours: 3,
      maxMatches: 50,
      playerRetries: 2,
      knownAccountId,
    });

    // 등수 스냅샷 캡처는 폴링의 본 목적(매치 기록)에 딸린 부수 효과다 —
    // 실패해도 폴링 응답 자체는 정상으로 돌려준다(사용자는 "폴링 성공"만
    // 신경 쓴다). 이미 캡처됐으면 captureRankingSnapshotForRoster가 알아서
    // 아무것도 안 하므로, 여러 번 폴링해도 안전하다.
    if (rosterId) {
      const roundCount = await countRoundsForRoster(supabase, rosterId);
      if (roundCount >= 4) {
        await captureRankingSnapshotForRoster(supabase, rosterId).catch(() => {});
      }
    }

    return NextResponse.json({ found: result.scrimsFound > 0 });
```

- [ ] **Step 5: 타입체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 6: 커밋**

```bash
git add app/api/ranking-snapshots/capture/route.ts app/api/ranking-snapshots/capture/route.test.ts app/api/scrim-roster/round-sheet/poll/route.ts
git commit -m "feat: capture ranking snapshot when a scrim session's 4th round lands"
```

---

## Task 6: 리더보드에 등수 변화 화살표/NEW 표시

**Files:**
- Modify: `components/dashboard/TierRankingPodium.tsx`
- Modify: `components/dashboard/TierRankingPodium.test.tsx`
- Modify: `app/dashboard/page.tsx`

**Interfaces:**
- Consumes: `RankingSnapshotRow`, `computeRankChange`, `fetchRankingSnapshots`(Task 4, `lib/rankingSnapshot.ts`).

- [ ] **Step 1: 실패하는 테스트 작성 — `TierRankingPodium.test.tsx`**

파일 끝에 추가:

```tsx
describe('TierRankingPodium — 등수 변화', () => {
  it('종합점수 탭에서 이전보다 등수가 오르면 빨간 상승 표시를 낸다', () => {
    // recent16/전체 기준 D가 1위 — 스냅샷에서 D가 3위였다면 2계단 상승.
    const snapshots = [{ window: 'recent16' as const, groupId: 'all', memberId: 'd', rankPosition: 3 }];
    render(<TierRankingPodium recent16={RECENT16} alltime={ALLTIME} snapshots={snapshots} />);
    expect(screen.getByText('▲2')).toBeInTheDocument();
  });

  it('스냅샷에 없던 사람은 NEW를 보여준다', () => {
    render(<TierRankingPodium recent16={RECENT16} alltime={ALLTIME} snapshots={[]} />);
    expect(within(screen.getByTestId('podium-slot-1')).getByText('NEW')).toBeInTheDocument();
  });

  it('평균킬 탭에서는 스냅샷이 있어도 변화 표시를 안 한다', () => {
    const snapshots = [{ window: 'recent16' as const, groupId: 'all', memberId: 'd', rankPosition: 3 }];
    render(<TierRankingPodium recent16={RECENT16} alltime={ALLTIME} snapshots={snapshots} />);
    fireEvent.click(screen.getByRole('button', { name: '평균킬' }));
    expect(screen.queryByText('▲2')).not.toBeInTheDocument();
    expect(screen.queryByText('NEW')).not.toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run components/dashboard/TierRankingPodium.test.tsx -t "등수 변화"`
Expected: FAIL — `snapshots` prop이 아직 없고 화살표 렌더링도 없음.

- [ ] **Step 3: 구현 — `TierRankingPodium.tsx`**

import 블록에 추가:

```ts
import { computeRankChange, type RankingSnapshotRow } from '@/lib/rankingSnapshot';
```

`useState`만 있던 React import에 `useMemo` 추가:

```ts
import { Fragment, useEffect, useMemo, useState } from 'react';
```

`TierRankingPodiumProps`에 `snapshots` 추가:

```ts
export interface TierRankingPodiumProps {
  recent16: RankingStatsRow[];
  alltime: RankingStatsRow[];
  snapshots: RankingSnapshotRow[];
}
```

컴포넌트 시그니처와 본문에 스냅샷 조회 맵 추가:

```ts
export function TierRankingPodium({ recent16, alltime, snapshots }: TierRankingPodiumProps) {
  ...
  const snapshotRankByMember = useMemo(() => {
    const map = new Map<string, number>();
    for (const snapshot of snapshots) {
      if (snapshot.window === activeWindow && snapshot.groupId === activeGroupId) {
        map.set(snapshot.memberId, snapshot.rankPosition);
      }
    }
    return map;
  }, [snapshots, activeWindow, activeGroupId]);
```

`badgeLabel` 함수 아래(또는 파일 아무 컴포넌트 함수 옆)에 배지 컴포넌트를 추가:

```tsx
function RankChangeBadge({ current, previous }: { current: number; previous: number | undefined }) {
  const change = computeRankChange(current, previous);
  if (!change) return null;
  if (change.type === 'new') {
    return (
      <span
        className="ml-2 text-[10px] font-bold"
        style={{ color: '#39FF88', textShadow: '0 0 6px rgba(57,255,136,0.8)' }}
      >
        NEW
      </span>
    );
  }
  const isUp = change.type === 'up';
  return (
    <span className={`ml-2 text-xs font-bold ${isUp ? 'text-red-400' : 'text-blue-400'}`}>
      {isUp ? '▲' : '▼'}
      {change.delta}
    </span>
  );
}
```

시상대(podium) 점수 블록에 배지를 끼워 넣는다 — 기존 `<MetricValue ... stacked />` 바로 아래:

```tsx
<p className="mt-5 tabular-nums">
  <MetricValue
    metric={activeMetric}
    row={member}
    className="text-2xl font-bold text-foreground sm:text-3xl"
    detailClassName="text-sm font-normal text-menu"
    stacked
  />
</p>
{activeMetric === 'rageScore' && (
  <RankChangeBadge current={slot.rank} previous={snapshotRankByMember.get(member.memberId)} />
)}
```

Task 2에서 새로 만든 4위 이하 표 행 렌더링의 점수 칸(`<span className="text-right tabular-nums">...`)을 아래로 교체한다:

```tsx
<span className="flex items-center justify-end gap-1 tabular-nums">
  <MetricValue
    metric={activeMetric}
    row={member}
    className="text-sm font-bold text-foreground"
    detailClassName="text-xs font-normal text-menu"
  />
  {activeMetric === 'rageScore' && (
    <RankChangeBadge current={originalRank} previous={snapshotRankByMember.get(member.memberId)} />
  )}
</span>
```

- [ ] **Step 4: `app/dashboard/page.tsx`에 스냅샷 fetch 연결**

```tsx
import { fetchRankingSnapshots } from '@/lib/rankingSnapshot';

export default async function DashboardPage() {
  const [recent16, alltime, snapshots] = await Promise.all([
    fetchRankingStats('recent16'),
    fetchRankingStats('alltime'),
    fetchRankingSnapshots(),
  ]);

  return (
    <main className="min-h-screen">
      <Nav />
      <h1 className="sr-only">{siteConfig.dashboard.pageHeading}</h1>
      <TierRankingPodium recent16={recent16} alltime={alltime} snapshots={snapshots} />
      <Footer />
    </main>
  );
}
```

- [ ] **Step 5: 기존 테스트가 새 필수 prop 때문에 깨지지 않게 보정**

Task 1/2에서 만든 다른 `<TierRankingPodium .../>` 렌더 호출(관리자 검색 테스트 포함, 이번 태스크의 새 테스트를 뺀 전부)에 `snapshots={[]}`를 추가한다 — TypeScript가 필수 prop 누락을 컴파일 에러로 잡아준다. `npx tsc --noEmit`이 지적하는 위치를 그대로 고치면 된다.

- [ ] **Step 6: 테스트 + 타입체크**

Run: `npx tsc --noEmit && npx vitest run components/dashboard/TierRankingPodium.test.tsx`
Expected: 타입 에러 없음, 전체 PASS

- [ ] **Step 7: 커밋**

```bash
git add components/dashboard/TierRankingPodium.tsx components/dashboard/TierRankingPodium.test.tsx app/dashboard/page.tsx
git commit -m "feat: show rank change arrows and NEW badge on leaderboard (rageScore only)"
```

---

## Task 7: 01 네임플레이트 종합점수 배지

**Files:**
- Create: `app/api/rage-scores/route.ts`
- Create: `app/api/rage-scores/route.test.ts`
- Modify: `components/team-builder/RosterBoard.tsx`
- Modify: `components/team-builder/RosterBoard.test.tsx`

**Interfaces:**
- Consumes: `TIER_SCORE_BANDS`, `RAGE_SCORE_STEEPNESS`, `eligibleForRanking`, `fetchRankingStats`, `rageScores`(Task 1, `lib/rankingStats.ts`), `useAdmin`(`components/admin/AdminProvider.tsx`).
- Produces: `GET /api/rage-scores?window=recent16|alltime` → `{ scores: Record<string, number> }`.

- [ ] **Step 1: API 라우트 구현 — `app/api/rage-scores/route.ts`**

```ts
import { NextResponse } from 'next/server';
import {
  RAGE_SCORE_STEEPNESS,
  TIER_SCORE_BANDS,
  eligibleForRanking,
  fetchRankingStats,
  rageScores,
  type RankingWindow,
} from '@/lib/rankingStats';

// team-builder "01 티어 테이블"이 관리자 전용 종합점수 배지를 그리는 데 쓴다.
// RosterBoard는 다른 데이터를 전부 REST 라우트로만 받으므로(브라우저에서
// Supabase를 직접 안 부름), 여기서도 같은 패턴을 따른다.
export async function GET(request: Request) {
  const windowParam = new URL(request.url).searchParams.get('window');
  const window: RankingWindow = windowParam === 'alltime' ? 'alltime' : 'recent16';

  try {
    const rows = await fetchRankingStats(window);
    const eligible = eligibleForRanking(rows);
    const scored = rageScores(eligible, TIER_SCORE_BANDS, RAGE_SCORE_STEEPNESS);
    const scores: Record<string, number> = {};
    for (const row of scored) scores[row.memberId] = row.score;
    return NextResponse.json({ scores });
  } catch (error) {
    const message = error instanceof Error ? error.message : '점수를 불러오지 못했습니다.';
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
```

- [ ] **Step 2: 라우트 테스트 — `app/api/rage-scores/route.test.ts`**

```ts
import { describe, expect, it, vi } from 'vitest';
import { GET } from './route';

vi.mock('@/lib/rankingStats', async (importOriginal) => {
  const actual = await importOriginal<typeof import('@/lib/rankingStats')>();
  return {
    ...actual,
    fetchRankingStats: vi.fn().mockResolvedValue([
      {
        memberId: 'a',
        discordNickname: 'A',
        tier: 0,
        totalGameCount: 20,
        windowGameCount: 20,
        avgKills: 3,
        avgPlacementPoints: 8,
        avgRank: 2,
        lastPlayedAt: new Date().toISOString(),
      },
    ]),
  };
});

describe('GET /api/rage-scores', () => {
  it('memberId를 키로 하는 점수 맵을 낸다', async () => {
    const response = await GET(new Request('http://localhost/api/rage-scores?window=recent16'));
    const body = await response.json();
    expect(typeof body.scores.a).toBe('number');
  });
});
```

- [ ] **Step 3: 테스트 실행**

Run: `npx vitest run app/api/rage-scores/route.test.ts`
Expected: PASS

- [ ] **Step 4: `RosterBoard.tsx`에 관리자 점수 fetch + Nameplate 배지 연결**

import 블록에 추가:

```tsx
import { useAdmin } from '@/components/admin/AdminProvider';
```

`Nameplate` 함수 시그니처에 `score` prop 추가(기존 `onDelete` 다음):

```tsx
function Nameplate({
  entry,
  dragging = false,
  draggable = true,
  fixed = false,
  width,
  score,
  onDragStart,
  onDragEnd,
  onClick,
  onDelete,
}: {
  entry: RosterEntry;
  dragging?: boolean;
  draggable?: boolean;
  fixed?: boolean;
  width?: number;
  // 관리자에게만, 01 티어 칸에서만 넘긴다(최근 16매치 종합점수) — 02 표는
  // 이미 고정 배색 등으로 정보가 많아서 이 배지를 안 더한다.
  score?: number;
  onDragStart?: (event: DragEvent<HTMLElement>) => void;
  onDragEnd?: () => void;
  onClick?: () => void;
  onDelete?: () => void;
}) {
```

`return (<div className="relative">...)` 블록 안, `{onDelete && (...)}` 다음에 배지를 추가한다:

```tsx
  return (
    <div className="relative">
      {plate}
      {entry.vipRank !== null && <VipCrown />}
      {score !== undefined && (
        <span
          className="pointer-events-none absolute -bottom-1.5 -left-1.5 rounded bg-black/70 px-1 text-[10px] font-bold text-accent"
        >
          {score.toFixed(1)}
        </span>
      )}
      {onDelete && (
        <button
          type="button"
          onClick={(event) => {
            event.preventDefault();
            event.stopPropagation();
            onDelete();
          }}
          aria-label={`${name} 삭제`}
          className="absolute right-1 top-1/2 -translate-y-1/2 text-base leading-none text-white/70 hover:text-red-400"
        >
          ×
        </button>
      )}
    </div>
  );
}
```

`RosterBoard` 함수 안, 다른 `useState` 선언들 옆에 추가:

```tsx
export function RosterBoard({ roster }: { roster: Roster | null }) {
  const router = useRouter();
  const { isAdmin } = useAdmin();
  const [scoreByMemberId, setScoreByMemberId] = useState<Record<string, number>>({});
  ...
```

`if (!roster) { return ... }` 아래, `rosterId` 선언 근처에 admin 전용 fetch용 `useEffect`를 추가한다. `useState`만 import돼 있으므로 React import 줄에 `useEffect`를 더한다:

```tsx
import { useEffect, useState, type DragEvent, type FormEvent, type MouseEvent } from 'react';
```

```tsx
useEffect(() => {
  if (!isAdmin) {
    setScoreByMemberId({});
    return;
  }
  let cancelled = false;
  fetch('/api/rage-scores?window=recent16')
    .then((res) => res.json())
    .then((body) => {
      if (!cancelled) setScoreByMemberId(body.scores ?? {});
    })
    .catch(() => {});
  return () => {
    cancelled = true;
  };
}, [isAdmin]);
```

(이 `useEffect`는 `if (!roster) return <p>...</p>;` **위**, 컴포넌트 최상단 다른 훅들과 나란히 둔다 — React 훅은 조건부 return보다 먼저 호출돼야 한다. 기존 코드도 이미 이 순서를 따르고 있다.)

01 티어 칸 Nameplate(파일에서 `<Nameplate` 첫 번째 호출, `entries.filter((entry) => entry.tierSlot === slot)` 다음 블록)에 `score` 전달:

```tsx
<Nameplate
  entry={entry}
  dragging={entry.id === draggingId}
  score={entry.memberId ? scoreByMemberId[entry.memberId] : undefined}
  onDragStart={(event) => {
    event.dataTransfer.effectAllowed = 'move';
    setDraggingId(entry.id);
  }}
  onDragEnd={clearDragState}
  onDelete={() => void handleDeleteEntry(entry.id)}
/>
```

보류(unassigned) Nameplate(두 번째 호출)에도 동일하게:

```tsx
<Nameplate
  entry={entry}
  dragging={entry.id === draggingId}
  width={NAMEPLATE_WIDTH}
  score={entry.memberId ? scoreByMemberId[entry.memberId] : undefined}
  onDragStart={(event) => {
    event.dataTransfer.effectAllowed = 'move';
    setDraggingId(entry.id);
  }}
  onDragEnd={clearDragState}
  onDelete={() => void handleDeleteEntry(entry.id)}
/>
```

02 표 Nameplate(세 번째 호출, `<table>` 안)는 그대로 둔다 — `score`를 안 넘긴다.

- [ ] **Step 5: 테스트 작성 — `RosterBoard.test.tsx`**

파일 끝에 추가. `vi.mock`으로 `useAdmin`을 관리자로 바꾸는 방법은 `vitest.setup.ts`가 이미 `useAdmin`을 `vi.fn()`으로 목 잡아두므로 `vi.mocked`로 값을 바꾸면 된다:

```tsx
import { useAdmin } from '@/components/admin/AdminProvider';

describe('RosterBoard - 01 종합점수 배지(관리자 전용)', () => {
  it('일반 사용자에게는 점수가 안 보인다', () => {
    const roster = makeRoster([makeEntry({ id: 'a', tierSlot: 1, memberId: 'mem-a' })]);
    render(<RosterBoard roster={roster} />);
    expect(screen.queryByText(/^\d+\.\d$/)).not.toBeInTheDocument();
  });

  it('관리자에게는 매칭된 멤버의 종합점수가 보인다', async () => {
    vi.mocked(useAdmin).mockReturnValue({ isAdmin: true, login: vi.fn(), logout: vi.fn() });
    vi.stubGlobal(
      'fetch',
      vi.fn().mockResolvedValue({ ok: true, json: async () => ({ scores: { 'mem-a': 82.3 } }) }),
    );

    const roster = makeRoster([makeEntry({ id: 'a', tierSlot: 1, memberId: 'mem-a' })]);
    render(<RosterBoard roster={roster} />);

    await waitFor(() => expect(screen.getByText('82.3')).toBeInTheDocument());
    expect(fetch).toHaveBeenCalledWith('/api/rage-scores?window=recent16');
  });
});
```

- [ ] **Step 6: 테스트 실행**

Run: `npx vitest run components/team-builder/RosterBoard.test.tsx`
Expected: 전체 PASS

- [ ] **Step 7: 타입체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 8: 커밋**

```bash
git add app/api/rage-scores/route.ts app/api/rage-scores/route.test.ts components/team-builder/RosterBoard.tsx components/team-builder/RosterBoard.test.tsx
git commit -m "feat: show admin-only recent-16-match rage score badge on 01 nameplates"
```

---

## Task 8: 브라우저 검증

**Files:** 없음(코드 변경 없음 — `preview_start`로 dev 서버 띄워 실제 화면 확인).

- [ ] **Step 1: dev 서버 기동**

`preview_start`(`{ name: 'dev' }`, 없으면 `.claude/launch.json`에 `npm run dev` 설정 추가)로 `/dashboard`를 연다.

- [ ] **Step 2: 리더보드 확인**

- "최근 16매치" 탭 라벨이 보이는지, 물음표 툴팁에 "최근 내전 4회"가 뜨는지 확인.
- 관리자 암구호로 로그인 후(`AdminLoginButton`) 4위 이하 표 헤더 바로 위에 검색창이 오른쪽 정렬로 뜨는지, 검색어를 입력하면 필터링되는지 확인.
- 등수 변화 화살표/NEW 배지는 아직 스냅샷 데이터가 없으면 전원 NEW로 뜬다 — 정상이다(첫 캡처 전이므로). `ranking_snapshots`에 임시로 몇 행 넣어 상승/하락이 실제로 색과 화살표 방향대로 나오는지 한 번 확인하고 지운다.

- [ ] **Step 3: team-builder 01 확인**

`/team-builder`를 열고 명단이 업로드된 상태에서, 관리자로 로그인했을 때만 각 네임플레이트 왼쪽 아래에 점수 배지가 뜨는지, 닉네임·X 삭제 버튼·VIP 왕관과 안 겹치는지 확인. 로그아웃하면 배지가 사라지는지도 확인.

- [ ] **Step 4: 콘솔/네트워크 확인**

`read_console_messages`로 에러 없는지, `read_network_requests`로 `/api/rage-scores`·`/api/ranking-snapshots/capture` 응답이 200인지 확인.

- [ ] **Step 5: 스크린샷으로 사용자에게 결과 공유**

`computer` 스크린샷 액션으로 리더보드(검색+화살표)와 01 화면(점수 배지) 각각 한 장씩 캡처해 사용자에게 보여준다.

---

## Self-Review 메모

- **스펙 커버리지**: 1(16매치)=Task1, 2(관리자 검색)=Task2, 3(등수 변화, 스키마+캡처+화면)=Task3~6, 4(01 점수 배지)=Task7. 전부 매핑됨.
- **타입 일관성**: `RankingWindow`('recent16'|'alltime')를 `lib/rankingStats.ts`에서 한 번만 정의하고 `lib/rankingSnapshot.ts`·API 라우트·컴포넌트가 전부 그 타입을 재사용한다 — 별도로 문자열 리터럴을 다시 안 만든다. `RankingSnapshotRow.groupId`와 `TierGroup.id`(둘 다 `string`)가 그대로 비교 가능하다.
- **순서 의존성**: Task 3(스키마)이 Task 4(캡처 함수)보다 먼저 와야 마이그레이션이 실제 DB에 있는 상태에서 캡처 함수를 테스트/수동 확인할 수 있다. Task 6(화면)은 Task 4의 `computeRankChange`/`fetchRankingSnapshots`에 의존하므로 그 다음.
