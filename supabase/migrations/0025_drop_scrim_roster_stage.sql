-- 01/02/03 화면이 전부 항상 보이는 구조로 바뀌면서(팀 구성이 안 됐으면 03 표가
-- 빈 칸으로 보일 뿐, 특정 단계를 지나야 다음 화면이 열리는 게임이 아니게 됐다)
-- "새로고침해도 몇 단계까지 왔는지 기억"할 이유가 사라졌다. 0020에서 추가한
-- 컬럼을 그대로 지운다.

alter table scrim_rosters drop column if exists stage;
