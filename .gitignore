.claude/settings.local.json
.claude/launch.json
.claude/worktrees/
.superpowers/
node_modules/
.next/
.env
.env.local
scripts/match-sample.json
data/
tsconfig.tsbuildinfo
supabase/.env.functions
supabase/.temp/

# 이미지 판독용 임시 폴더 (스크린샷 백필 작업)
public/_crop/

# image/ 안의 지금 파일들은 위 커밋으로 이미 스냅샷 떠뒀다 — 앞으로 여기에
# 추가되는 새 파일만 git status에서 안 보이게 가린다(추적 중인 기존 파일은
# 그대로 추적된다).
image/
