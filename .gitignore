.claude/settings.local.json
.claude/launch.json
.claude/worktrees/
.superpowers/
node_modules/
.next/
.env
.env.local
scripts/match-sample.json
data/*
# 우승 판독 결과만 예외로 추적한다. 종합 시트 24장을 사람이 눈으로 읽어 만든
# 값이라 잃으면 다시 읽는 수밖에 없다 — data/ 의 다른 파일들과 달리 실명이
# 없고(인게임 닉네임뿐) 크기도 작다.
!data/session-winners.json

# 같은 이유로 추적한다 — 최근 10회 종합 시트에서 1~16위를 눈으로 읽은 값이고,
# 잃으면 시트 10장을 다시 판독해야 한다. 인게임 닉네임뿐이라 실명이 없다.
!data/session-standings.json
tsconfig.tsbuildinfo
supabase/.env.functions
supabase/.temp/

# 이미지 판독용 임시 폴더 (스크린샷 백필 작업)
public/_crop/

# image/ 안의 지금 파일들은 위 커밋으로 이미 스냅샷 떠뒀다 — 앞으로 여기에
# 추가되는 새 파일만 git status에서 안 보이게 가린다(추적 중인 기존 파일은
# 그대로 추적된다).
image/
