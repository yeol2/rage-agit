// 트로피 글리프 하나. 리더보드 포디움 배지와 클랜원 화면의 우승 트로피가
// 같은 그림을 써야 해서 여기 한 곳에만 둔다.
//
// 전부 채운(solid) 한 덩어리로 그렸다 — 컵만 채우고 손잡이·기둥을 가는 선으로
// 두면 굵기가 따로 놀아 어색하다.

// 글리프 실제 경계(x 2.6~21.4, y 3.5~21)에 맞춰 조인 값이다. 기본 `0 0 24 24`
// 를 쓰면 사방 여백 때문에 같은 svg 크기라도 트로피가 작아 보인다.
export const TROPHY_VIEWBOX = '2.6 2.85 18.8 18.8';
export const TROPHY_X = 2.6;
export const TROPHY_Y = 2.85;
export const TROPHY_SIZE = 18.8;

// 우승 트로피 황금색 — 위쪽이 밝고 아래로 갈수록 짙어져 금속처럼 보인다.
// 쓰는 쪽이 같은 문서에 여러 개를 그릴 수 있으므로 id 를 밖에서 정해 넘긴다.
export function TrophyGoldGradient({ id }: { id: string }) {
  return (
    <linearGradient id={id} x1="0" y1="0" x2="0.35" y2="1">
      <stop offset="0%" stopColor="#FFF1BE" />
      <stop offset="30%" stopColor="#FFD365" />
      <stop offset="62%" stopColor="#E8A62F" />
      <stop offset="100%" stopColor="#B0741A" />
    </linearGradient>
  );
}

export function TrophyPaths() {
  return (
    <>
      {/* 컵 */}
      <path d="M6.5 3.5H17.5V8C17.5 11.59 15.09 14.5 12 14.5C8.91 14.5 6.5 11.59 6.5 8Z" />
      {/* 왼쪽 손잡이 */}
      <path d="M6.5 4.6H4C3.23 4.6 2.6 5.23 2.6 6V7.6C2.6 10.3 4.5 12.5 7 13.1V11.4C5.3 10.8 4.3 9.4 4.3 7.6V6.3H6.5Z" />
      {/* 오른쪽 손잡이 */}
      <path d="M17.5 4.6H20C20.77 4.6 21.4 5.23 21.4 6V7.6C21.4 10.3 19.5 12.5 17 13.1V11.4C18.7 10.8 19.7 9.4 19.7 7.6V6.3H17.5Z" />
      {/* 기둥 — 폭 1.7 (x=12 기준 대칭). 아래 끝은 받침 윗변(18.7)보다 살짝 내려
          19.2 까지 내려서 받침과 빈틈 없이 붙게 한다(같은 색이라 겹침은 안 보인다). */}
      <path d="M11.15 13.6H12.85V19.2H11.15Z" />
      {/* 받침 — 두께 2.3 (바닥 y=21 고정, 윗변 18.7). 모서리 반경 0.9 */}
      <path d="M7.3 18.7H16.7C17.2 18.7 17.6 19.1 17.6 19.6V20.1C17.6 20.6 17.2 21 16.7 21H7.3C6.8 21 6.4 20.6 6.4 20.1V19.6C6.4 19.1 6.8 18.7 7.3 18.7Z" />
    </>
  );
}
